// PositionTexture
// EffectRadius
// WorldPosition


for (int i = 0; i < Texture; i++) 
{
	float4 position = SAMPLE_TEXTURE2D(PositionTexture, Sampler, float2(0, i));
	float4 sign = SAMPLE_TEXTURE2D(PositionTexture, Sampler, float2(1, i));

	position.x = position.x * 2550 * (sign.x - 0.5) * 2
	position.y = position.y * 2550 * (sign.y - 0.5) * 2
	position.z = position.z * 2550 * (sign.z - 0.5) * 2

	float dist = distance(WorldPosition, position)
	if (dist < EffectRadius) {
		
	}
}


